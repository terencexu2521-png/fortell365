name: Test Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Hello World
      run: echo "Hello World"

    - name: Create test file
      run: |
        mkdir -p public/seo
        echo "Test" > public/seo/test.html

    - name: List files
      run: |
        ls -la public/seo/
